//Данный класс представляет собой линейный список свободных звеньев, из
//которого перегруженными операторами new / delete будет выделяться память под
//новое звено иерархического списка.

class TMemoryManager {
	//Zveno* pFirst;// Указатель на первое звено памяти
	//Zveno* pLast;// Указатель на последнее звено памяти
	//Zveno* pFree;// Указатель на первое свободное звено
	//friend class Zveno;
};